using System;
using System.Collections.Generic;
using System.Linq;

namespace List
{
    class Program
    {
        static void Main(string[] args)
        {

        /*    char[] chars = {'a','b','c' };
            string[] tokens = Console.ReadLine().Split("\\s+");
            double[] numArr = Console.ReadLine().Split("\\s+").Select(double.Parse).ToArray();

            foreach (var item in numArr)
            {
                Console.WriteLine($"{Math.Pow(item, numArr[2])}");
            }

            List<string> words = Console.ReadLine().Split("\\s+").ToList();

            List<int> nums = Console.ReadLine().Split("\\s+").Select(int.Parse).ToList();
        */
            List<String> hardCore = new List<string>() { "me", "and", "you", "will", "die" };
            Console.WriteLine(hardCore[1] + " " + hardCore[0]);
            var dictionary = new Dictionary<String, String>();


        }
    }
}
